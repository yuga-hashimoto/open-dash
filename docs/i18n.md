---
title: Internationalisation (i18n)
---

# Internationalisation

OpenDash ships UI strings in 44 locales plus a "system default" entry,
modelled on openclaw-assistant's per-app language override.

## Shipped locales

**This table previously listed only 27 locales; the real count is 44 —
`app/src/main/res/values-*/` on disk is ground truth, not this table.
Verify with `ls app/src/main/res | grep '^values-'` if in doubt.**

| BCP-47 tag | Resource dir | Label |
|---|---|---|
| *(system default)* | `values/` (English fallback) | System default |
| `en` | `values/` | English |
| `ja` | `values-ja/` | 日本語 |
| `ar` | `values-ar/` | العربية (RTL) |
| `bg` | `values-bg/` | Български |
| `bn` | `values-bn/` | বাংলা |
| `ca` | `values-ca/` | Català |
| `cs` | `values-cs/` | Čeština |
| `da` | `values-da/` | Dansk |
| `de` | `values-de/` | Deutsch |
| `el` | `values-el/` | Ελληνικά |
| `es` | `values-es/` | Español |
| `et` | `values-et/` | Eesti |
| `fa` | `values-fa/` | فارسی (RTL) |
| `fi` | `values-fi/` | Suomi |
| `fil` | `values-fil/` | Filipino |
| `fr` | `values-fr/` | Français |
| `hi` | `values-hi/` | हिन्दी |
| `hr` | `values-hr/` | Hrvatski |
| `hu` | `values-hu/` | Magyar |
| `in` | `values-in/` | Bahasa Indonesia |
| `it` | `values-it/` | Italiano |
| `iw` | `values-iw/` | עברית (RTL) |
| `kk` | `values-kk/` | Қазақ тілі |
| `ko` | `values-ko/` | 한국어 |
| `lt` | `values-lt/` | Lietuvių |
| `lv` | `values-lv/` | Latviešu |
| `ms` | `values-ms/` | Bahasa Melayu |
| `nb` | `values-nb/` | Norsk bokmål |
| `nl` | `values-nl/` | Nederlands |
| `pl` | `values-pl/` | Polski |
| `pt-BR` | `values-pt-rBR/` | Português (Brasil) |
| `ro` | `values-ro/` | Română |
| `ru` | `values-ru/` | Русский |
| `sk` | `values-sk/` | Slovenčina |
| `sl` | `values-sl/` | Slovenščina |
| `sr` | `values-sr/` | Српски |
| `sv` | `values-sv/` | Svenska |
| `ta` | `values-ta/` | தமிழ் |
| `th` | `values-th/` | ไทย |
| `tr` | `values-tr/` | Türkçe |
| `uk` | `values-uk/` | Українська |
| `ur` | `values-ur/` | اردو (RTL) |
| `vi` | `values-vi/` | Tiếng Việt |
| `zh-CN` | `values-zh-rCN/` | 简体中文 |
| `zh-TW` | `values-zh-rTW/` | 繁體中文 |

The base `values/strings.xml` is English by default. Every shipped
locale directory mirrors the exact same string names — if you add a
new string, add it to every locale file in the same PR so translators
aren't guessing which strings are new. `LocaleStringsParityTest`
enforces key parity (string *names* match) at CI time — it does not
check translation quality. In practice only `ja` carries real
translations end-to-end; the other 42 non-English locale files are
largely English placeholder text copied verbatim to satisfy the parity
check, real translation work still outstanding. Keep this in mind
before assuming a `values-<tag>/strings.xml` file is actually
localized — check its content, not just its existence.

## How override works

- Preference key `APP_LOCALE_TAG` stores the BCP-47 tag. Blank = follow
  system locale (the default).
- `LocaleManager.apply(tag)` persists the choice AND routes it to the
  Android 13+ platform `LocaleManager.applicationLocales`, so the change
  takes effect immediately and is mirrored in Settings → System →
  Languages → OpenDash.
- On Android 12 (API 31) and below, the platform API does not exist.
  We persist the preference for when the user upgrades, but the UI
  follows the system locale. The Settings picker hides (or disables)
  itself in that case.
- `OpenDashApp.onCreate` reads the saved tag and replays the
  platform call at process start, so a cold launch on a rebooted device
  still comes up in the user's chosen language.

## Voice control of the picker

The LLM can change the UI locale via the `LocaleToolExecutor` tools:

- `get_app_locale` — returns current tag + label + override-supported flag
- `list_app_locales` — enumerates bundled options
- `set_app_locale` — accepts a bundled tag, rejects others with the
  valid list in the error so the agent recovers in one round

Utterances like "switch to Japanese" or "日本語にして" are intended to
route through the `LocaleSwitchMatcher` fast-path once it lands —
until then they reach the LLM, which picks the right tool on its own.

## Voice-interaction language

This page covers the **UI chrome** only. Voice input/output languages
are controlled separately:

- `STT_LANGUAGE` — recogniser locale; see Settings → Voice.
- `TTS_LANGUAGE` — speech-output locale.
- Skills carry JA + EN triggers natively; the fast-path router regexes
  don't depend on `APP_LOCALE_TAG`.

A future PR may cross-wire them so that selecting Japanese for the UI
also flips STT/TTS — today they're independent on purpose so the user
can keep English voice input while reading the UI in their native
language.

## Adding a new locale

1. Copy `app/src/main/res/values/strings.xml` to
   `app/src/main/res/values-<tag>/strings.xml`.
2. Translate every string value. Keep XML element names verbatim — only
   the text inside `<string>` tags changes.
3. Preserve `app_name` as "OpenDash" — the brand name is not
   localised.
4. Add the tag to `LocaleManager.options`; the label should render in
   its own language (e.g. "Deutsch", not "German") so the picker is
   self-descriptive.
5. Confirm `./gradlew :app:mergeDebugResources` succeeds — Android's
   resource merger catches a missing translation by throwing.
6. Add a row in the table above.

## Reference

Stolen from openclaw-assistant's `SettingsActivity` picker +
`applySavedAppLocale()` — see their `applySavedAppLocale` for the
same persist-then-apply idiom we mirror.

- `app/src/main/java/com/opendash/app/util/LocaleManager.kt`
- `app/src/main/java/com/opendash/app/data/preferences/PreferenceKeys.kt`
- `app/src/main/res/values*/strings.xml`
